{% set primary = primary ?? [] %}
{% set secondary = secondary ?? [] %}

<div class="menu grid bg-dark s-pad-top-1 s-pad-bottom-2">
    <div class="s-col-12">
        {% include 'components/menu-items/menu-items' with { primary: primary, secondary: secondary } only %}
    </div>
</div>