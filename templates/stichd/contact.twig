{% extends 'stichd/layout' %}
{% block content %}
    {% for location in entry.locations %}
        {% set props = location | merge({ borderBottom: loop.index < entry.locations | length }) %}
        {% include 'components/contact/contact' with props only %}
    {% endfor %}
{% endblock %}

