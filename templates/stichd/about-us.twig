{% extends 'stichd/layout' %}
{% block content %}

    <h1 class="hidden">{{ entry.title }}</h1>

    <div class="s-pad-top-1">
        {% include 'components/blocks/blocks' with {'blocks': entry.blocks.all } %}
    </div>
{% endblock %}